<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>油气 & 固井每日洞察 | Petroleum Insights</title>
    <style>
        :root {
            /* 调整为更符合油气行业的配色：深灰、石油黑、安东红 */
            --primary-color: #2c3e50;
            --accent-color: #c0392b; /* 类似安东红 */
            --bg-color: #ecf0f1;
            --card-bg: #ffffff;
            --text-secondary: #7f8c8d;
        }

        body {
            font-family: 'Segoe UI', 'Microsoft YaHei', sans-serif;
            background-color: var(--bg-color);
            background-image: radial-gradient(#bdc3c7 1px, transparent 1px);
            background-size: 20px 20px;
            color: var(--primary-color);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        .container {
            background-color: var(--card-bg);
            padding: 40px;
            border-radius: 8px; /* 更加硬朗的圆角 */
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            max-width: 650px;
            width: 100%;
            border-top: 5px solid var(--accent-color); /* 顶部红色装饰条 */
        }

        h1 {
            font-size: 26px;
            margin-bottom: 5px;
            color: #333;
            font-weight: 800;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        h1 span {
            font-size: 30px;
        }

        .subtitle {
            font-size: 14px;
            color: var(--text-secondary);
            margin-bottom: 25px;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-bottom: 1px solid #eee;
            padding-bottom: 15px;
        }

        .fact-content {
            font-size: 20px;
            line-height: 1.5;
            margin-bottom: 20px;
            font-weight: bold;
            color: #000;
        }

        .fact-details {
            font-size: 15px;
            color: #444;
            line-height: 1.7;
            text-align: left;
            background: #fdfdfd;
            padding: 20px;
            border: 1px solid #e0e0e0;
            border-left: 4px solid var(--primary-color);
        }
        
        .tag {
            display: inline-block;
            background: #eee;
            padding: 2px 8px;
            border-radius: 4px;
            font-size: 12px;
            color: #555;
            margin-bottom: 10px;
            font-weight: 600;
        }

        button {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            font-size: 14px;
            cursor: pointer;
            margin-top: 25px;
            transition: background 0.2s;
            font-weight: 600;
        }

        button:hover {
            background-color: #1a252f;
        }
        
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #95a5a6;
            text-align: right;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1><span>🛢️</span> 每日油气洞察</h1>
        <div class="subtitle" id="dateDisplay">DATE</div>
        
        <div id="tagDisplay" class="tag">Category</div>
        <div class="fact-content" id="factTitle">
            读取数据中...
        </div>
        
        <div class="fact-details" id="factDetail">
            正在从地层深处提取数据...
        </div>

        <button onclick="loadRandomFact()">查看下一条 (Next Insight)</button>
        
        <div class="footer">
            Curated by Ryan (Anton Oil)
        </div>
    </div>

    <script>
        // 行业定制数据库：涵盖固井、钻井历史、中东市场
        const industryTrivia = [
            {
                category: "历史 / History",
                title: "为什么一桶石油是 42 加仑？",
                detail: "1866年，宾夕法尼亚州的石油商们开会决定标准。他们选择了当时用于运输威士忌和葡萄酒的 42 加仑木桶（tierce）。这比传统的 40 加仑桶多了 2 加仑，目的是为了弥补运输过程中的溢出损耗。"
            },
            {
                category: "固井 / Cementing",
                title: "固井水泥其实怕“糖”",
                detail: "糖（蔗糖）是一种强效的水泥缓凝剂。如果不小心把一罐可乐倒进水泥浆里，可能会导致水泥长时间无法凝固。在某些极端作业事故处理中，人们甚至会故意泵入糖水来阻止水泥在管柱内凝固。"
            },
            {
                category: "科威特 / Market",
                title: "大布尔干油田的传奇",
                detail: "科威特的 Burgan Field 是世界第二大油田。1938年发现时，钻井人员其实并没有钻很深，主力油层深度仅在 3500-4500 英尺左右，且具有惊人的自喷能力，这在当今的超深井时代简直是神话。"
            },
            {
                category: "钻井 / Drilling",
                title: "世界上最深的井挖到了什么？",
                detail: "苏联的科拉超深钻孔（SG-3）深达 12,262 米。那里没有地狱，但温度高达 180°C。钻头遇到岩石时，岩石表现得像塑料一样（塑性流动），这使得钻探无法继续进行。"
            },
            {
                category: "固井 / Cementing",
                title: "波特兰水泥的名字由来",
                detail: "固井常用的 G 级水泥源自波特兰水泥。1824年英国人 Joseph Aspdin 发明它时，觉得硬化后的颜色很像英国波特兰岛上开采的建筑石材，因此得名，跟美国的波特兰市毫无关系。"
            },
            {
                category: "环境 / HSE",
                title: "最早的“钻井液”其实是水",
                detail: "但在现代，我们常说“泥浆（Mud）”。有趣的是，早期的旋转钻井确实使用牛群踩踏出来的泥水混合物来糊住井壁。现在的油基泥浆（OBM）虽然昂贵且环保要求高，但对于科威特这种易坍塌地层是必不可少的。"
            },
             {
                category: "伊拉克 / Iraq",
                title: "基尔库克的“永恒之火”",
                detail: "伊拉克的 Baba Gurgur 油田有着持续燃烧了 4000 年的天然气火。这被认为是《圣经》中描述的“烈火窑”的原型，也是古代当地人获取沥青的地方。"
            }
        ];

        function updateDate() {
            const now = new Date();
            const options = { year: 'numeric', month: 'long', day: 'numeric', weekday: 'short' };
            document.getElementById('dateDisplay').textContent = now.toLocaleDateString('en-US', options).toUpperCase();
        }

        function getDailyFact() {
            const now = new Date();
            const start = new Date(now.getFullYear(), 0, 0);
            const diff = now - start;
            const oneDay = 1000 * 60 * 60 * 24;
            const dayOfYear = Math.floor(diff / oneDay);
            const index = dayOfYear % industryTrivia.length;
            renderFact(index);
        }

        function renderFact(index) {
            const fact = industryTrivia[index];
            document.getElementById('tagDisplay').textContent = fact.category;
            document.getElementById('factTitle').textContent = fact.title;
            document.getElementById('factDetail').textContent = fact.detail;
            
            // 根据分类改变标签颜色 (简单视觉区分)
            const tag = document.getElementById('tagDisplay');
            if(fact.category.includes("Cementing")) tag.style.color = "#c0392b";
            else if(fact.category.includes("History")) tag.style.color = "#2980b9";
            else tag.style.color = "#555";
        }

        function loadRandomFact() {
            const randomIndex = Math.floor(Math.random() * industryTrivia.length);
            renderFact(randomIndex);
        }

        updateDate();
        getDailyFact();

    </script>
</body>
</html>